import Spinner from "../../ui/Spinner/Spinner";
import "./pageLoader.css";

function PageLoader() {
  return (
    <div className="wempa-page-loader">
      <Spinner />
    </div>
  );
}

export default PageLoader;